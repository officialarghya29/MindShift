# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# experiment artifacts (keep results JSON + demo report)
evaluation/run.log

# OS / editor
.DS_Store
.idea/
.vscode/

# large transient data (regenerable deterministically)
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

notebooks/.ipynb_checkpoints/

# local-only raw-text transfer copy (PS-01 §40 data minimization)
evaluation/results/transfer_goemotions_full.json

# fine-tune engine artifact stays local-only
evaluation/results/finetune_engine.joblib
models/saved/finetune_engine.joblib
